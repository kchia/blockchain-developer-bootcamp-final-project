import { ProjectsList } from "../../features";
import { Button } from "../../common/core";

export default function HomePage() {
  return (
    <section>
      <h2>Create unique, one-of-a-kind bubble art</h2>
      <Button text="connect to web3" />
    </section>
  );
}
